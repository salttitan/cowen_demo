import { Typography } from "@mui/material";

const RoundOneCohort = () => {
  return (
    <Typography variant="body1">
      From here we would go through similar steps for the cohort model, followed
      by the unit(s) and solo(s) and eventually into Round Two for Maintenance
      Phase, Charges, and melee attacks.
    </Typography>
  );
};
export default RoundOneCohort;
